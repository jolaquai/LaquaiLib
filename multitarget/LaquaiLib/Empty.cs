namespace LaquaiLib;

/// <summary>
/// Implements an empty <see langword="struct"/>. Used in union definitions as a distinction for "no value" as opposed to <see langword="null"/> acting as a sentinel.
/// </summary>
public readonly struct Empty;
